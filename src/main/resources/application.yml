spring:
  application:
    name: imobflow-api

  datasource:
    url: jdbc:mysql://localhost:3306/imobflow
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration